/**
 * The collapsed dock badge pulses briefly on new activity. Behind a flag
 * because a pulse on every page is exactly the kind of thing that grates after
 * a week — flip this to false and it is gone.
 */
export const DOCK_PULSE_ENABLED = true;
